## Process this file with automake to produce Makefile.in

CLEANFILES = *~
MAINTAINERCLEANFILES = Makefile.in

modprobe_DATA = 50-blacklist.conf 60-filesystem-blacklist.conf

EXTRA_DIST = $(modprobe_DATA)

